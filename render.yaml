services:
  - type: web
    name: bellah-whatsappbot
    env: node
    buildCommand: npm install
    startCommand: bash start.sh
    plan: free
    region: frankfurt
    branch: main
    autoDeploy: true
