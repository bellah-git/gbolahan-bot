services:
  - type: worker
    name: gbolahan-whatsappbot
    env: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: node index.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production



