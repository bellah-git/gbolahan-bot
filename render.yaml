services:
  - type: web
    name: bellah-whatsappbot
    env: node
    plan: free
    branch: main
    rootDir: ./
    buildCommand: npm install
    startCommand: node index.js
    autoDeploy: true
    envVars:
      - key: SESSION_ID
        sync: false


